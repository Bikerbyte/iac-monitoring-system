---
name: ian-dotnet-style-zh-tw
description: 延續 Ian 在 ASP.NET Core、Razor Pages、內部工具、背景工作與維運腳本上的實作風格。當需求是新增或修改內部系統功能，且希望保留實務導向、可維護、好交接的寫法，同時仍然遵守目標 repo 既有慣例時使用。
---

# Ian 寫作風格

## 目標

請用一種真的在維護內部系統的人會採用的方式寫程式，避免過度模板化或過度抽象化。

重點是：

- 可讀
- 可維護
- 可追 log
- 可快速補需求
- 能保留業務脈絡

不要一味追求架構很漂亮，優先確保接手的人看得懂、找得到問題、改得動。

套用到其他 repo 時，請先遵守該 repo 的既有規則：

- 先比對該 repo 的命名、排版、分層、檔案結構，再決定怎麼套用這份風格。
- 這份文件是傾向，不是凌駕於 repo 慣例之上的硬模板。
- 如果目標 repo 的標準比這份更嚴格，優先跟 repo。

## 整體感覺

- 程式要像是在解決真實團隊問題。
- 可以接受適度重複，只要這樣能讓流程更直接、更容易理解。
- 不要為了看起來高級，就把本來很清楚的邏輯拆得太碎。
- 比起炫技，這種風格更重視「直接、有脈絡、出了事知道去哪裡找」。

## 實作守則

- 一個 request handler 通常只處理一個主要操作。
- 有副作用的 post 操作，如果重新整理整頁對使用者較清楚，通常優先 redirect。
- 設定載入通常集中在 `Program.cs` 或專案既有的 startup 做法，不在各個 service 各自重建。
- 註解優先說明用途、例外、維運背景，不解釋一眼就看懂的程式動作。
- 若相同邏輯已重複三次以上，應重新評估是否抽成 helper、service 或共用區塊。

## 結構習慣

- `Program.cs` 保持清楚，讓人一眼就能看出：
  - 設定載入
  - DI 註冊
  - 驗證與授權
  - Session
  - HostedService
  - Middleware 流程
- `Service` 類別負責真正的外部整合或核心處理，例如：
  - LDAP / AD
  - 憑證掃描
  - 權限判斷
  - 排程背景工作
  - Excel / 檔案處理
- `PageModel` 主要處理單一頁面的請求流程與畫面資料準備。
- `.cshtml` 盡量維持直白，讓 UI 區塊、按鈕、資料表、提示訊息都看得清楚。

## 命名風格

- 偏好直白、功能導向的命名，不刻意炫技巧。
- 方法名稱要看得出用途，例如：
  - `AuthenticateWithEnv`
  - `OnPostBatchRescan`
  - `QueryByUsername`
  - `GetCertTypeDisplay`
- Page handler 命名維持 Razor Pages 慣例，讓人一看就知道是哪個操作。
- 可以自然混用英文識別字與中文註解、中文畫面文案，但仍以目標 repo 的主要語言為優先。

## 註解風格

- 註解是這個風格很重要的一部分，但不要為了註解而註解。
- 註解主要用途包括：
  - 說明區塊用途
  - 標記流程段落
  - 補充商業規則
  - 留下修改背景
  - 記錄某次需求調整或特例原因
- 常見形式可保留：
  - `// Add Daily run rescan`
  - `// Func: Multi-ADs Test`
  - `// 顯示現有資料`
  - `/// OnPost 新增憑證，執行掃描`
  - `// Development 測試 DailyRescan 使用`
- 註解語氣以直接、實務、方便維護為主，不要寫成教科書。
- 不要預設每個區塊都要加註解，只有在註解真的有補充價值時才寫。

## 錯誤處理與防呆

- 優先做早期檢查，像是：
  - 參數為空
  - 設定缺失
  - 權限不足
  - 查無資料
- 系統邊界附近要做好 `try/catch`，尤其是：
  - LDAP / AD
  - DB
  - 檔案
  - 憑證
  - 外部服務
- 出錯時要有足夠資訊可以追，但不需要把錯誤模型設計得很複雜。
- 在內部工具情境下，簡單明確的成功/失敗訊息通常比過度包裝更實用。

## Logging 習慣

- 請保留足夠的 log，讓維運或除錯時能知道發生了什麼事。
- 特別適合加 log 的地方：
  - 登入與授權流程
  - 背景排程執行
  - 掃描結果
  - 批次處理
  - 外部依賴錯誤
- log 訊息不用寫得很官腔，重點是讓人看得懂、查得到。
- 若使用者會看到相對應的結果訊息，可以讓 log 與畫面訊息有一定對應性。

## Razor Pages 習慣

- 表單輸入偏好使用 `[BindProperty]`。
- `OnGet()` 負責初始化顯示資料。
- `OnPost()` 或 `OnPostXxx()` 專注處理單一操作。
- 成功或失敗可用 `TempData` 帶回前端提示。
- 若操作後重新整理整頁比較合理，偏好 `RedirectToPage(...)`。
- 一個 PageModel 若有多個操作，可以用註解切出區塊，不急著硬拆很多類別。

## Service 寫法

- Service 可以是具體類別，不一定要先抽 interface。
- 只有在測試替換、擴充需求真的明確時，才需要多做一層抽象。
- 回傳型別可以務實，例如：
  - `Dictionary<string, string>`
  - 簡單 DTO
  - 給 UI 直接吃的結果物件
- 寫法以明確分支、容易閱讀為優先，不要為了通用化做過多包裝。

## Console / Script / Background Job 習慣

- 背景工作要明確寫出：
  - 何時跑
  - 跑什麼
  - 更新什麼
  - 成功幾筆、失敗幾筆
- Console 工具與 PowerShell 腳本要有清楚的執行輸出，讓人不進 debugger 也能知道流程跑到哪。
- 腳本可以保留大區塊註解、流程分隔線、操作說明，這是風格的一部分。

## 前端與畫面呈現

- 以前端內部系統為主，重點不是炫，而是清楚、好操作。
- Bootstrap 為主沒有問題。
- `.cshtml` 中保留區塊註解是合理的。
- 畫面上偏好明確可視化提示，例如：
  - alert
  - badge
  - loading
  - grouped table
  - 明確的按鈕名稱
- 小型頁面互動可以直接放少量 inline script，不一定每次都抽到外部檔案。
- 若目標 repo 是對外產品、多人協作前端或已有明確前端規範，優先跟隨該 repo 的前端慣例。

## 避免的事情

- 不要硬改成很制式的 clean architecture 範本。
- 不要把有脈絡的註解全部清掉。
- 不要把原本好懂的流程過度 generic 化。
- 不要把所有重複都視為壞事。
- 不要把文字修得太像公關稿、顧問文件或過度均質化的風格。
- 不要讓新加的 code 看起來像是外來框架強壓進來的。

## 要保留的脈絡感

- 保留專案記憶，例如：
  - 區塊註解
  - 特例說明
  - 環境提示
  - 日期類型的修改痕跡
- 保留中英混合的真實工作語境，但仍以 repo 既有語言為優先。
- 保留「這段是在處理哪個實務問題」的痕跡，而不是只剩純技術抽象。
- 新增功能時，請讓人感覺像是在原本工具上自然擴充，而不是整套重寫。
- 作者痕跡、日期註記、背景補充請節制使用，只在對後續維護真的有幫助時保留。

## 未來在其他 Repo 的套用方式

- 先觀察附近既有寫法，再延續，不要先帶入自己的理想模板。
- 增量修改優於全面翻新。
- 若真的要抽共用邏輯，只抽那種已經明顯重複、而且抽出來後會更好懂的部分。
- 若有兩種寫法都可行，優先選比較直白、比較容易交接的那一種。

## 快速檢查清單

- `Program.cs` 是否一眼看得懂註冊與管線流程
- `Service` 是否真的在承載有意義的外部整合或業務處理
- `PageModel` 是否圍繞單一請求操作展開
- 是否自然保留工作語境並尊重 repo 主語言
- 是否有足夠的區塊註解協助掃描
- 是否在高風險流程留下 log
- 是否用簡單明確的方式回饋成功或失敗
- 是否保留了真實維運工具該有的直接感
