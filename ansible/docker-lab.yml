---
- name: Deploy local Docker monitoring simulation
  hosts: docker_lab_stack
  gather_facts: false

  vars:
    docker_lab_stack_dir: "{{ playbook_dir }}/../.docker-lab/stack"
    docker_lab_prometheus_image: prom/prometheus:v2.54.1
    docker_lab_grafana_image: grafana/grafana:11.2.0
    docker_lab_blackbox_image: prom/blackbox-exporter:v0.25.0
    docker_lab_grafana_admin_user: admin
    docker_lab_grafana_admin_password: admin

  tasks:
    - name: Create local Docker lab stack directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ docker_lab_stack_dir }}"
        - "{{ docker_lab_stack_dir }}/grafana"
        - "{{ docker_lab_stack_dir }}/grafana/provisioning"
        - "{{ docker_lab_stack_dir }}/grafana/provisioning/datasources"
        - "{{ docker_lab_stack_dir }}/grafana/provisioning/dashboards"
        - "{{ docker_lab_stack_dir }}/grafana/dashboards"

    - name: Ensure Docker lab network exists
      ansible.builtin.shell: |
        docker network inspect "{{ iac_docker_lab_network }}" >/dev/null 2>&1 || docker network create "{{ iac_docker_lab_network }}"
      changed_when: false

    - name: Deploy Docker lab Prometheus config
      ansible.builtin.template:
        src: templates/docker-lab-prometheus.yml.j2
        dest: "{{ docker_lab_stack_dir }}/prometheus.yml"
        mode: "0644"
      notify: Restart Docker lab Prometheus

    - name: Deploy Docker lab Grafana datasource
      ansible.builtin.copy:
        src: files/docker-lab/grafana/provisioning/datasources/prometheus.yml
        dest: "{{ docker_lab_stack_dir }}/grafana/provisioning/datasources/prometheus.yml"
        mode: "0644"
      notify: Restart Docker lab Grafana

    - name: Deploy Docker lab Grafana dashboard provider
      ansible.builtin.copy:
        src: files/docker-lab/grafana/provisioning/dashboards/dashboard.yml
        dest: "{{ docker_lab_stack_dir }}/grafana/provisioning/dashboards/dashboard.yml"
        mode: "0644"
      notify: Restart Docker lab Grafana

    - name: Deploy Docker lab dashboard
      ansible.builtin.copy:
        src: files/docker-lab/grafana/dashboards/docker-lab-overview.json
        dest: "{{ docker_lab_stack_dir }}/grafana/dashboards/docker-lab-overview.json"
        mode: "0644"
      notify: Restart Docker lab Grafana

    - name: Check blackbox exporter container state
      ansible.builtin.shell: |
        docker inspect -f '{% raw %}{{.State.Running}}{% endraw %}' iac-lab-blackbox 2>/dev/null || true
      register: blackbox_state
      changed_when: false

    - name: Start blackbox exporter container
      ansible.builtin.shell: |
        docker rm -f iac-lab-blackbox >/dev/null 2>&1 || true
        docker run -d \
          --name iac-lab-blackbox \
          --restart unless-stopped \
          --network "{{ iac_docker_lab_network }}" \
          -p "{{ iac_docker_lab_blackbox_port }}:9115" \
          "{{ docker_lab_blackbox_image }}"
      when: blackbox_state.stdout != "true"
      changed_when: true

    - name: Check Prometheus container state
      ansible.builtin.shell: |
        docker inspect -f '{% raw %}{{.State.Running}}{% endraw %}' iac-lab-prometheus 2>/dev/null || true
      register: prometheus_state
      changed_when: false

    - name: Start Prometheus container
      ansible.builtin.shell: |
        docker rm -f iac-lab-prometheus >/dev/null 2>&1 || true
        docker run -d \
          --name iac-lab-prometheus \
          --restart unless-stopped \
          --network "{{ iac_docker_lab_network }}" \
          -p "{{ iac_docker_lab_prometheus_port }}:9090" \
          -v "{{ docker_lab_stack_dir }}/prometheus.yml:/etc/prometheus/prometheus.yml:ro" \
          "{{ docker_lab_prometheus_image }}" \
          --config.file=/etc/prometheus/prometheus.yml \
          --storage.tsdb.retention.time=2d
      when: prometheus_state.stdout != "true"
      changed_when: true

    - name: Check Grafana container state
      ansible.builtin.shell: |
        docker inspect -f '{% raw %}{{.State.Running}}{% endraw %}' iac-lab-grafana 2>/dev/null || true
      register: grafana_state
      changed_when: false

    - name: Start Grafana container
      ansible.builtin.shell: |
        docker rm -f iac-lab-grafana >/dev/null 2>&1 || true
        docker run -d \
          --name iac-lab-grafana \
          --restart unless-stopped \
          --network "{{ iac_docker_lab_network }}" \
          -p "{{ iac_docker_lab_grafana_port }}:3000" \
          -e GF_SECURITY_ADMIN_USER="{{ docker_lab_grafana_admin_user }}" \
          -e GF_SECURITY_ADMIN_PASSWORD="{{ docker_lab_grafana_admin_password }}" \
          -e GF_USERS_ALLOW_SIGN_UP=false \
          -v "{{ docker_lab_stack_dir }}/grafana/provisioning:/etc/grafana/provisioning:ro" \
          -v "{{ docker_lab_stack_dir }}/grafana/dashboards:/var/lib/grafana/dashboards:ro" \
          "{{ docker_lab_grafana_image }}"
      when: grafana_state.stdout != "true"
      changed_when: true

  handlers:
    - name: Restart Docker lab Prometheus
      ansible.builtin.shell: |
        docker rm -f iac-lab-prometheus >/dev/null 2>&1 || true
        docker run -d \
          --name iac-lab-prometheus \
          --restart unless-stopped \
          --network "{{ iac_docker_lab_network }}" \
          -p "{{ iac_docker_lab_prometheus_port }}:9090" \
          -v "{{ docker_lab_stack_dir }}/prometheus.yml:/etc/prometheus/prometheus.yml:ro" \
          "{{ docker_lab_prometheus_image }}" \
          --config.file=/etc/prometheus/prometheus.yml \
          --storage.tsdb.retention.time=2d
      changed_when: true

    - name: Restart Docker lab Grafana
      ansible.builtin.shell: |
        docker rm -f iac-lab-grafana >/dev/null 2>&1 || true
        docker run -d \
          --name iac-lab-grafana \
          --restart unless-stopped \
          --network "{{ iac_docker_lab_network }}" \
          -p "{{ iac_docker_lab_grafana_port }}:3000" \
          -e GF_SECURITY_ADMIN_USER="{{ docker_lab_grafana_admin_user }}" \
          -e GF_SECURITY_ADMIN_PASSWORD="{{ docker_lab_grafana_admin_password }}" \
          -e GF_USERS_ALLOW_SIGN_UP=false \
          -v "{{ docker_lab_stack_dir }}/grafana/provisioning:/etc/grafana/provisioning:ro" \
          -v "{{ docker_lab_stack_dir }}/grafana/dashboards:/var/lib/grafana/dashboards:ro" \
          "{{ docker_lab_grafana_image }}"
      changed_when: true
